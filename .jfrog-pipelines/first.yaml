pipelines:
  - name: Shashank's pipeline
    steps: 
      - name: step1
        type: Bash
        execution:
          onExecute:
            - echo "onExecute Command Invoked"
          onSuccess:
            - echo "onSuccess Command Invoked"
          onComplete:
            - echo "onComplete Command Invoked"
